```
/src/main/java/com/example/project
│── controller/ # Handles HTTP requests (similar to Node.js controllers)
│── service/ # Business logic (similar to Node.js services)
│── repository/ # Handles database operations (similar to Node.js models)
│── model/ # Defines entities (similar to Node.js models)
│── dto/ # Data Transfer Objects (optional, for API requests/responses)
│── config/ # Configuration files (security, CORS, etc.)
│── exception/ # Custom exceptions (optional)
│── util/ # Utility classes (helper methods, constants)
└── Application.java # The main entry point (like `index.js` in Node.js)
```
